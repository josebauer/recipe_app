import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: "bold",
    paddingTop: 32
  },
  subtitle: {
    fontWeight: "bold",
    marginTop: 20,
    marginBottom: 10
  }
});

export default styles;
